# 프로젝트 작업 지침

## 로컬 서버와 내부 공유

- 사용자가 확인할 개발 서버와 미리보기 서버는 기본적으로 `0.0.0.0`에 바인딩하여 같은 내부 네트워크에서 접속할 수 있게 실행한다. `127.0.0.1`이나 `localhost`에만 바인딩하지 않는다.
- 실행 후 실제 사설 IPv4 주소와 포트를 확인하고 접속 가능한 URL을 사용자에게 제공한다. 주소를 추측하거나 `0.0.0.0`을 접속 주소로 안내하지 않는다.
- 이 기본 동작은 매번 사용자에게 재확인하지 않는다. 사용자가 명시적으로 로컬 전용 실행을 요청하면 그 요청을 따른다.
- 이미지 출력 및 자동 검사에만 사용하고 종료하는 내부 서버는 루프백 바인딩을 유지할 수 있다.
- 방화벽 변경이나 라우터 포트 포워딩은 이 지침에 포함되지 않는다.

## 게시 이미지 주소

- 기존 채용공고에서 사용하는 public/downloads/VW-{A,B,C,PM-A,PM-B,PM-C}-3x.png 주소를 유지한다. 공통 이미지도 같은 파일에 출력하여 이미지 재배포만으로 기존 공고를 갱신한다. -3x는 호환용 파일명이며 현재 출력 배율과 별개다.
- PC/MO 별도 이미지나 새로운 파일명으로 게시 URL을 바꾸지 않는다. 사이트 미리보기용 -preview.png는 유지한다.

- 게시용 공고 이미지에는 접수 시작일·마감일·채용 기간·채용 시 마감 문구를 넣지 않는다. 해당 영역은 “지원 방법”과 “잡코리아 이력서 양식으로 즉시지원”만 표시하며, 접수 기간은 잡코리아 공고의 접수기간 영역에서 관리한다.

- 줄바꿈은 최종 게시 PNG 기준으로 검수한다. 한글 어절과 역명 등을 중간에서 끊지 않고, 조사·어미 또는 짧은 단어만 다음 줄에 남지 않도록 의미 단위로 조정한다.
